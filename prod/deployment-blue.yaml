apiVersion: apps/v1
kind: Deployment
metadata:
  name: cafe-static-blue
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cafe-static
      version: blue
  template:
    metadata:
      labels:
        app: cafe-static
        version: blue
    spec:
      containers:
      - name: cafe-static
        image: image-registry.openshift-image-registry.svc:5000/ci/cafe-static:latest
        ports:
        - containerPort: 8080
